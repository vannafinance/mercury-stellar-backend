# Copilot Test Run — Open Items

**Branch:** `copilot-ui-rewire` · **Date:** 2026-08-12

Everything already fixed, verified, or passed this run has been dropped. This document
lists only what remains open, with severity, location, and a proposed fix for each.

---

## 1. Code Fixes Needed

| # | Issue | Severity | Location | Proposed Fix |
|---|-------|----------|----------|---------------|
| 1 | "when XLM hits $0.50 sell everything" still falls to the generic capabilities blurb — not fixed, and out of scope for a quick fix. | Low, needs product decision | `lib/copilot/router.ts` | There is no `sell` / liquidate-everything op at all, so no amount of trigger-word tuning routes this to a write for the guard below to catch. Needs a real op (and its own risk/confirmation path) before a conditional wrapped around it means anything. |

**Item resolved without a code change:** "Single-leg deposit intermittently routes as `multi_leg`" ("deposit 5 XLM as collateral" failed once tagged `multi_leg`, then succeeded plain seconds later). Re-investigated from scratch: traced every `multi_leg: true` site in `router.ts`, `shouldLlmPlan`, and `looksLikeMultiGoal` — none can match this exact phrase, and `keywordConfident` means Vertex is never even consulted for it, so nothing in the current code path can produce a non-deterministic result. Ran it 5× through the deterministic router (identical output every time) and 3× live through the actual browser UI in fresh chats (clean single-leg execution every time, no error). Most likely already fixed by one of the many other routing fixes that landed earlier this session, or was a transient Vertex-side hiccup from before those landed. No code changed; closing as unreproducible rather than leaving it open indefinitely.

---

## 2. External — Not Fixable From This Repo

| # | Issue | Status | Notes |
|---|-------|--------|-------|
| 1 | `vanna_borrow` rejects any BLUSDC amount above a low threshold (~$5, sometimes higher), with no diagnostic beyond a generic message. Blocks any leveraged BLUSDC position. | Open — narrowed | MCP server logs (`vanna-mcp-server`, Cloud Run) show the real on-chain failure is `HostError: Error(Contract, #13)` on the token transfer *into* the smart account during `borrow()` — the same class of error as the trustline issue fixed in §3.4 below, just not proven to be the identical cause. `is_borrow_precheck` returns `true` (the risk gate is not the blocker); Soroban SAC balances held by a *contract* have no classic trustline "limit" field, so the size-dependent threshold is still unexplained. Needs `vanna_core`'s token-contract source (not in this repo or `vanna_mcp`) or direct ledger-state inspection to pin down further. |
| 2 | ~~Sign Service $1000/tx spend cap didn't hold once~~ | **Resolved — was not a Sign Service bug** | Root-caused via `vanna-sign-service` Cloud Run logs: the Sign Service correctly rejected the over-cap borrow **twice** (`over_per_tx_cap`, confirmed in its own logs), but this repo's `mcp-write.ts` mapped that policy rejection to `needs_wallet_sign` with the XDR still attached, and the client's own embedded-session-key auto-approve signed and submitted it anyway — bypassing the Sign Service entirely. Fixed in §3 below; the Sign Service itself needed no change. |

---

## 3. Fixed This Pass (MCP / Sign Service root causes, requested separately)

| # | Issue | Fix | Repo | Status |
|---|-------|-----|------|--------|
| 1 | **Critical** — a genuine Sign Service policy rejection (`over_per_tx_cap`, `over_daily_cap`, `contract_not_allowlisted`, `function_not_allowlisted`, `session_expired`, ...) was staged as `needs_wallet_sign`, and the client's embedded-session-key auto-approve silently signed and submitted it anyway. | `executeMcpWrite` now recognises `auto_sign: "rejected"` with a genuine policy reason and returns `status: "rejected"` (→ `risk.decision: "block"`, no XDR attached) instead of falling through to the generic "stage for signature" path. | vanna-copilot-orchestrator | ✅ Fixed, tested (6 new unit tests), pushed to `copilot-ui-rewire`, PR [#57](https://github.com/vannafinance/mercury-stellar-backend/pull/57) |
| 2 | HostError #13 (trustline missing) on a borrow always reported "XLM is not ready in your wallet", regardless of which asset actually hit the error — `classifyTrustlineFailure` is itself asset-aware, but its only call site never passed an asset, so `readinessDisplayAsset(null)` defaulted to `"XLM"` every time. | `humanizeMcpWriteError` now takes an `{asset, trader}` context and threads `step.args.symbol` through to `classifyTrustlineFailure`. | vanna-copilot-orchestrator | ✅ Fixed, tested (3 new unit tests), pushed, same PR as above |
| 3 | AQUA (and any symbol with no oracle feed) surfaced as a generic `contract_error` reading like a transient outage ("the price for AQUA is currently unavailable due to an oracle contract error") — `oracle.py`'s own exception message ("Oracle: price not found for symbol 'AQUA'") was already specific, but `error_handling.py`'s `tool_safe` flattened every `ContractCallError` into the same generic bucket regardless of message. | Added a `reason: "no_price_feed"` case matched on the wrapper's own message shape (not a per-symbol allowlist, so it stays correct if a symbol gains/loses a feed) — message now says plainly "X has no price feed on this oracle deployment... not transient, retrying will not help." | `vanna_mcp` (mercury-stellar-backend) | ✅ Fixed, tested (1 new unit test passing, full suite otherwise green besides 2 pre-existing unrelated failures — see below). **Committed locally on `main`, not pushed** — only the copilot repo's push/PR was requested; say if you want this pushed too. |
| 4 | Multi-leg leveraged positions could skip the approval card. "open a 3x position with 50 BLUSDC" auto-executed the deposit leg immediately with no preview — this phrasing hit `deposit_and_borrow`'s direct-execute + `pending_write.follow_up` chain, a second multi-leg mechanism separate from the `plan_preview`/freeze/approve flow that "deposit X and borrow Y at Nx" correctly used for the identical trade. | Both the `deposit_and_borrow` and leveraged `deploy_to_blend`/`supply_to_blend` handlers now build their steps and call a shared `freezeLeveragedPlanPreview` helper, which routes through the same freeze/fingerprint/approve flow as every other multi-leg plan, instead of calling `runWrite` directly on leg 1. | vanna-copilot-orchestrator | ✅ Fixed, tested, verified live end-to-end (preview card shown → approved → both legs executed), pushed to `copilot-ui-rewire` |
| 5 | No server-side idempotency on writes. The same `approved_plan`, or an identical write request, sent twice both executed independently — confirmed with two concurrent "lend 1 XLM" calls producing two real tx hashes. The UI's Run button disables while loading, so a normal double-click couldn't trigger this, but a retry, a second tab, or a replayed request below the UI layer still could. | New `write-dedupe.ts` claims a `trader+op+asset+amount` key (writes) or the `plan_id` (approved plans) for 8s; a repeat within that window is refused with "I won't submit it twice" instead of executing again. | vanna-copilot-orchestrator | ✅ Fixed, tested, verified live (two concurrent "lend 1 XLM" calls → first executed on-chain, second refused as duplicate), pushed to `copilot-ui-rewire` |
| 6 | "borrow the max I can safely" asked which USDC variant before asking for a size. Safe (never auto-executed), but the wrong question first — no asset was named at all, yet `asset ?? "USDC"` fed a literal `"USDC"` into the bare-USDC ambiguity gate, which cannot tell a manufactured default apart from a genuine "USDC". | When `findAsset()` finds no asset word at all, the borrow branch now returns a clarify asking for amount + asset together, instead of defaulting to `"USDC"` and falling into the variant gate. | vanna-copilot-orchestrator | ✅ Fixed, tested (4 new unit tests), verified live ("borrow the max I can safely" → "How much do you want to borrow, and in which asset?"), pushed to `copilot-ui-rewire` |
| 7 | **Escalated on investigation — was worse than "unhelpful".** "what's the XLM/BTC pool" fell to the generic capabilities blurb instead of naming BTC as unsupported. Separately, `conditional-guard.ts`'s CONDITIONAL trigger only recognised "if", not "when ... hits/reaches/drops/...". That second gap was not just unhelpful: "when my health factor drops below 1.2 repay 10 XLM" and "when XLM reaches $0.60 withdraw my collateral" both already routed to a real `repay` / `withdraw_collateral` write with no guard in front of them — under auto-sign these would have executed for real with the condition silently dropped, the exact class of bug this guard module exists to prevent. | Added "when ... hits/reaches/crosses/exceeds/above/below/drops/falls/rises/goes" to `CONDITIONAL`, and added `pool`/`price`/`rate`/`apy`/`apr`/`stats`/`worth` as trigger words on the unsupported-asset gate in `router.ts` so a read-style question naming a bad ticker gets the specific refusal instead of the generic blurb. | vanna-copilot-orchestrator | ✅ Fixed, tested (8 new unit tests), verified live (both phrasings now correctly refused — no live write was sent for the previously-unguarded cases), pushed to `copilot-ui-rewire` |
| 8 | **Architectural, not from the original test script.** The floating "Vanna Assistant" widget (docked on every page besides `/copilot`, meant to be a Gemini-Assist-style page guide) and the dedicated `/copilot` orchestrator page hit the exact same `/api/copilot` endpoint and the exact same `handleChat`, with no server-side distinction between them. A plain action sentence typed into the Assistant widget — "deposit 5 XLM as collateral", "lend 5 XLM", "open a 3x position with 50 BLUSDC" — fell through the page-guide classifier (`isAssistantChat`) and reached the full MCP write path, signing and submitting for real, identically to typing it on `/copilot`. | Added `surface: "assistant" \| "copilot"` to the request; the Assistant widget (`assistant-launcher.tsx`) now sends `"assistant"`, the Copilot page (`copilot-workspace.tsx`) sends `"copilot"`. Two gates in `handle.ts`: one refuses the structured write continuations (`approved_plan`/`auto_sign`/`pending_write`/`resume_multi_leg`) up front, the other refuses once routing classifies the message as `write`/`plan`/`auto_sign` — both return a message pointing to the Copilot page instead of executing. Reads, page-guide questions, and personal-account queries are untouched on the Assistant widget; `/copilot` is untouched entirely. | vanna-copilot-orchestrator | ✅ Fixed, tested (5 new unit tests), verified live across 4 pages (`/earn`, `/farm`, `/portfolio`, `/margin`) — guide questions and personal reads answer normally, single-leg and leveraged multi-leg writes are redirected with no execution; `/copilot`'s own write path reconfirmed unaffected (`lend 1 XLM` signed and submitted normally), pushed to `copilot-ui-rewire` |
| 9 | **Reported live** — "what is my total supply in earn section" executed a live `lend` write asking which USDC variant, instead of answering the read question it was. Root cause: the lend-write branch's exclusion list matched fixed substrings ("my supply", "total supplied") — "my **total** supply" broke the match since an adjective sits between the words. Separately, once excluded from the write branch, the holdings-read branch (`asksAboutHoldings`) only recognised "position"/"holdings"/"portfolio" words, so the message (and most everyday phrasings of "how much do I have here" — "show me my earn balance", "what's my total in farm", "how much do I have in farm", etc.) fell through to the generic capabilities blurb instead of answering. | Replaced the substring exclusion with a shape-based regex (question word or "my" within a short span of "supply"/"supplied", not an exact phrase), and broadened `asksAboutHoldings` to also match a quantity-question word + a first-person marker + a quantity noun (balance/total/earned/earning/have) + a named venue, in any order. | vanna-copilot-orchestrator | ✅ Fixed, tested (18 new unit tests covering the original phrase, 9 everyday variants, and regression checks against real writes/pool-stat/Blend-reserve reads), verified live (both the exact reported phrase and "how much do I have in farm" now answer with the real position, not the wrong write flow), pushed to `copilot-ui-rewire` |
| 10 | **Reported live** — "how much debt do I have" fell through to the generic capabilities blurb (same class of bug as #9, different fixed-phrase list); "what's my net worth" / "net value" / "net asset value" were wrongly **blocked** by the LLM domain firewall as off-topic chat, since neither the vocabulary list nor any allow-pattern recognised those words, and containing "what" alone is enough to hit the firewall's off-domain-question rule. | Generalised the debt-read branch the same way as #9 (shape-based regex instead of an exact-phrase list), added a scoped compound-phrase allow-pattern for "net worth/value/assets" to the domain firewall (deliberately not bare "net"/"value" — too broad), and extended `asksAboutHoldings` to recognise the same "net worth/value" phrasing as a positions question. | vanna-copilot-orchestrator | ✅ Fixed, tested, verified live with a real account (`GDW3B2...VJ52`) — "how much debt do I have" / "what's my net worth" / "what is my net asset value" all now answer correctly instead of blurb/block |
| 11 | **Reported live, real money-figure bug** — the "Net Value" shown in both the copilot chat's position summary and the Copilot page's account sidebar was mathematically identical to **gross collateral**, not collateral minus debt — for account `CAHLZMJ...`, it read "net value: $2716.10" while debt was $565.97, i.e. debt was never actually netted out at all. Root cause: `pos.totalValue` (`lib/margin-health.ts`) is defined as `netAvailableCollateral + totalBorrowedValue`, which algebraically always collapses back to `grossCollateralValue` — adding debt back cancels the subtraction that produced `netAvailableCollateral` in the first place. An existing unit test even asserted this exact tautology, so nothing caught it. | `handle.ts`'s "net value" facts/headline and `copilot-workspace.tsx`'s sidebar "net value" row now read `netAvailableCollateral` (the figure genuinely net of debt) instead of `totalValue`. `totalValue` itself is untouched — it is not labeled "net" anywhere else in the app. | vanna-copilot-orchestrator | ✅ Fixed, verified live with real account data — net value now correctly shows collateral − debt ($2715.66 − $565.80 = $2149.86) |
| 12 | **Reported live, real money-figure bug** — depositing 5 XLM as collateral (debt unchanged) projected the health factor to **worsen** (1.50 → 1.35) in the staged-action "Projected Impact" card, when adding collateral with no new debt can only ever raise health factor or leave it unchanged. | `lib/copilot/risk.ts`'s `hfFrom()` multiplied collateral by a `DEFAULT_LT` of 0.9 that exists nowhere in the actual product formula (`lib/margin-health.ts`: `collateral / debt`, no discount, confirmed by the Margin page's own displayed number). `hf_before` almost always comes from a real MCP/snapshot read and bypassed this, but `hf_after` — a hypothetical future state with no real read to fall back on — went through the discounted formula on **every single write**, so every projected post-action health factor across the whole app (deposit, withdraw, borrow, repay) was ~10% off. Changed `DEFAULT_LT` to `1.0`, matching the real formula everywhere it's used. | vanna-copilot-orchestrator | ✅ Fixed, tested (4 new unit tests — this module had zero prior coverage), verified live with real account data (`hf_before: 1.6258 → hf_after: 1.6261` for a 5 XLM deposit, correctly higher, not lower) |
| 13 | **Reported live** — "Can you provide my Earn positions" answered with a card explicitly badged **"MARGIN ACCOUNT"**, showing margin collateral/debt instead of Earn supply — a different pool from margin collateral even when the same token (XLM, BLUSDC, ...) sits in both at once. Root cause: `query_all_positions` (margin snapshot + Blend/Aquarius farm overview) is the only "my positions" answer that exists at all — Earn (vToken) supply was never fetched by anything, on any phrasing. Three further layered bugs found while fixing the first: (a) the new Earn read initially passed the wrong account (`smart_account`) to `vanna_get_vtoken_balance` — vTokens are minted to the trader's **G-wallet**, not the margin C-account (this exact class of bug already has a comment in `tool-args.ts` from a prior fix); (b) calling `vanna_get_vtoken_balance` for all 4 assets reliably timed out client-side even sequentially — root-caused to the MCP server's own Cloud Run resource limits, see below; (c) once (a) and (b) were fixed, the answer *still* reported zero for every asset on a wallet with real supply — the real response uses `human` (vToken share count) and `redeemable_human` (the underlying amount, matching the Earn page's own "Your Supply" column); my code was reading nonexistent fields (`balance_human`/`balance`), an assumption never actually confirmed against a live payload. | Added a dedicated `query_earn_position` route (same "names one venue" pattern as Blend/Aquarius) and a new `earnPositionsAnswer` handler that reads the vToken balance for every Earn-supported asset (XLM/BLUSDC/AQUSDC/SOUSDC), reusing `buildToolArgs`'s existing `holder = trader \|\| smart` logic, parses `redeemable_human` (falling back to `human`), fetches a live XLM price for its USD figure (stables are 1:1), and answers with `venue: "earn"` — never falling back to margin/farm data. Calls are sequential with one retry, not parallel. | vanna-copilot-orchestrator | ✅ Fixed, tested (6 unit tests, mock MCP response shape corrected to match the real `human`/`redeemable_human` fields), verified live end-to-end through the actual browser UI with real account data — "Your Earn positions — 3 supplied, ~$70.15 total" (XLM 20.0184 / $3.16, AQUSDC 14.9895 / $14.99, SOUSDC 52.0009 / $52.00, BLUSDC correctly omitted at zero), matching the Earn page's own displayed balances exactly |
| 14 | **Reported live** — "Margin Details are also Wrong and what is this box representing?" (screenshot showed a small square/box glyph rendered directly before the value on `BORROWED · BLUSDC` and `BORROWED · AQUSDC` facts). The glyph itself is `AnswerFact`'s intentional colorblind-accessible tone marker (`TONE_MARK` in `answer-view.tsx` — tone is carried by shape, not just color, since health factor and debt are the two figures where "is this bad" is the whole question). The bug: `allPositionsStructured` (`handle.ts`) tagged **every** `borrowed · X` fact with a flat `tone: "warn"`, unconditional on the account's actual risk — so a perfectly healthy account (HF ~4.8, later confirmed live) showed the same "warning" square on its debt lines as a genuinely stressed one, reading as "something is wrong here" when nothing was. The health-factor fact directly above it already derived its own tone from real HF thresholds (`bad` under 1.1, `warn` under 1.4, else `good`) — the borrowed facts just never shared it. The separate "Margin Details are also wrong" half of this report was investigated and traced to the same stale-deployment cause as the collateral double-count note below — this session's own account checks (including a live `HF 4.79, collateral $2726.61 − debt $569.21 = net $2157.41`) all computed correctly with no discrepancy. | Every `borrowed · X` fact now reuses the exact same HF-derived tone as the health-factor fact (`undefined` when healthy, matching neither a false "good" up-arrow nor a false "warn" square). | vanna-copilot-orchestrator | ✅ Fixed, tested (4 new unit tests pinning the tone at all three HF tiers, plus that it always matches the health-factor fact's own tone), verified live — the square is now absent from `BORROWED · XLM` / `BORROWED · BLUSDC` on the real HF-4.79 account, while the health-factor fact's own good-tone marker is unaffected |
| 16 | **Reported live** — issue #4: "After I provide the prompt 'Supply Liquidity in XLM Lending Pool' it should ask me how much XLM I have to supply" (re-clarified: not how much the user *wants*, but how much they *have*, so they can decide against a real number — matching the Earn page's own "Bal: 3134.68 XLM" next to its input). The amount-less lend/supply clarify only ever asked "How much X do you want to supply?" with an APY note, no balance figure anywhere. | The clarify now calls `vanna_get_wallet_balance` for the named asset (via the existing `walletBalanceForEarn` helper, already used elsewhere for this exact parsing) and appends "You have N SYMBOL in your wallet." when the read succeeds and is non-zero; a failed or zero read falls back to the plain question, never blocking the clarify. | vanna-copilot-orchestrator | ✅ Fixed, tested (2 new unit tests: balance shown for a funded wallet, balance note omitted — not blocking — for a zero one; added a realistic `vanna_get_wallet_balance` mock response since none existed before), verified live — "Supply Liquidity in XLM Lending Pool" now answers "How much XLM do you want to supply to the Vanna earn pool? You have 3,138.1772 XLM in your wallet. Current XLM earn supply APY is ~4.76%. e.g. …", matching the real wallet balance |
| 15 | **Reported live, via annotated screenshot** — the two cards under an answer (the readable headline+facts card from `AnswerView`, and the raw key-value `FactsGrid` card beneath it) rendered at different widths and different left edges, reading as two unrelated pieces of UI instead of one answer. Root cause was two separate layout issues: (a) `AnswerView`'s figures grid caps at `maxWidth: 620` by design, but `FactsGrid` (`copilot-workspace.tsx`) had no width cap at all and stretched to the full flex container; (b) even after matching the widths, `FactsGrid` and `ImpactPanel` were siblings of the icon-indented flex row that wraps `AnswerView`, not children of it — so they sat flush against the answer's icon instead of aligned under its text. | Capped `FactsGrid` at the same `maxWidth: 620`, and moved both `ImpactPanel` and `FactsGrid` inside the same indented column as `AnswerView`/the prose message, so every card in an answer now shares one left edge and one width. | vanna-copilot-orchestrator | ✅ Fixed, verified live (both cards now align exactly under the "Vanna Earn" badge and headline on a real "what are my earn positions" answer) |
| 17 | **Reported live** — issue #8: "What is Borrow APY of XLM Lending Pool?" was misread as an instruction and answered "How much do you want to borrow?" instead of naming the rate. | `router.ts`'s borrow-write branch already excluded "borrow apr" from matching as a write, but not "borrow apy" — a plain asymmetry, since APY and APR are the same kind of question. Added "borrow apy" alongside it. | vanna-copilot-orchestrator | ✅ Fixed, tested, verified live — now answers "The XLM Vanna earn pool has a borrow APR of 12.91%." instead of asking a borrow amount |
| 18 | **Reported live** — issue #9, two related bugs under one theme ("reply to what was asked, not extra info, never misread a question as a command"): (a) "What is Collateral Left Before Liquidation of my margin account?" was refused outright as a restricted keeper/liquidate action — the router's very first check matched the bare substring "liquidation of" with no way to tell a real command apart from a question naming the same word; (b) "What is Net Available Collateral & Net amount Borrowed of my margin account" fell through everything to the generic capabilities blurb — neither of the two existing single-figure read branches (the debt question's word-distance window, the "net worth/value" phrase) matched a compound "X & Y" question naming two different figures by two different names. | Added a shape-based exception to the restricted-liquidate gate (a leading question word, or "before/until/left before liquidation", reads as a threshold question, not a command — a genuine liquidate command never opens that way) and a new `query_margin_figure` route + `marginFigureAnswer` handler that checks each named figure (net available collateral, gross collateral, amount borrowed, collateral left before liquidation) independently against the whole message, then answers with ONLY those figures — never the full `query_all_positions` card. | vanna-copilot-orchestrator | ✅ Fixed, tested (7 new unit tests: both misrouting bugs, a regression guard that a genuine "liquidate my account" is still refused, and that the answer never includes unrequested figures), verified live — "collateral left before liquidation" now answers "$2098.16" instead of a restricted refusal, and the compound question answers "Net available collateral: $2154.96 · Amount borrowed: $567.86" with exactly those two facts |
| 19 | **Reported live** — issue #10's health-factor half: a staged "borrow 31.4698609 SOUSDC" leg (as part of a 3x-leveraged deposit+borrow) projected HF **1.43 → 1.28** with collateral $479.55 and debt $305.69 → $337.19 — numbers that don't reduce to 1.43/1.28 under the product's real `collateral / debt` formula (1.57 → 1.42). Recomputing with the OLD, already-fixed `DEFAULT_LT = 0.9` discount from finding #12 above reproduces it exactly: `479.55 × 0.9 / 337.19 = 1.2800`, an exact match to the screenshot's "1.28", and `479.55 × 0.9 / 305.69 = 1.41` is within rounding of the screenshot's "1.43". Same root cause, same conclusion as the collateral-double-count note below: **this is a stale-deployment screenshot predating the #12 fix**, not a bug in current code. Verified with a fresh live test on the real account (deposit 20 XLM + borrow 5 BLUSDC at 2x): HF 4.80 → 4.767, exactly matching `collateral / debt` with no discrepancy at either step. | No code change — already fixed by #12. | vanna-copilot-orchestrator | ✅ Confirmed already fixed, verified live with a fresh controlled test |
| — | **Investigated, not a bug** — issue #10's second half: "we have Deposit and Borrow functionality, why did this take 2 transactions?" A real, atomic `vanna_deposit_and_borrow` MCP tool does exist, but `mcp-write.ts`'s `deposit_and_borrow` case deliberately does NOT call it — its own comment: "Prefer sequential deposit → borrow… Combined MCP tool is kept only as an explicit atomic fallback; its pre-flight ignores same-tx deposit." Calling the atomic tool would evaluate the borrow leg's eligibility against the PRE-deposit collateral, which can spuriously reject a leveraged borrow that only clears once the same-transaction deposit lands. | No change — this is a deliberate, already-documented tradeoff, not a regression. | vanna-copilot-orchestrator | ℹ️ By design — 2 sequential transactions (with 2 signatures, both clearly disclosed in the plan preview) avoids a real eligibility bug in the atomic alternative |
| 20 | **Reported live, via annotated screenshot** — the new `query_margin_figure` answer (#18) had two of its own display bugs: (a) a compound 2-figure answer's second row ("amount borrowed") rendered visibly shorter/narrower than the first ("net available collateral"), values not aligned to the same right edge; (b) the headline read as vague filler ("Here's what you asked for: net available collateral $X, amount borrowed $Y") instead of naming the figures directly. Root cause of (a): `AnswerView`'s figures grid only forced a FULL-width row for the fact whose OWN label exceeds 22 characters — with one long label ("net available collateral", 25 chars) and one short one ("amount borrowed", 16 chars) in the same answer, one row went full-width and the other stayed at the grid's half-width, so their values landed at two different x-positions. | Made the grid single-column for the WHOLE answer whenever ANY fact's label is long, not just that fact's own row — every row now shares the same width whenever mixed label lengths would otherwise misalign them. Rewrote the multi-figure headline as `"Label: Value  ·  Label: Value"`, mirroring the facts card underneath. | vanna-copilot-orchestrator | ✅ Fixed, verified live — "Net available collateral: $2154.96 · Amount borrowed: $567.86" with both card rows now aligned at the same right edge |
| 21 | **Reported live** — issue #12: "Now Can you Perform Swap From XLM to SoUSDC in Soroswap" fell through to the generic capabilities blurb instead of executing/asking to size a real swap — "it did not perform Spot Page swaps." Root cause: the swap branch required an explicit amount or balance fraction (`amount != null \|\| swapShare != null`) just to be RECOGNISED as a swap at all, unlike every other write op in this router (borrow, lend, deposit all classify the op first and separately flag `requires_amount`) — a swap naming both tokens and a venue but no size matched neither swap condition and was never classified as anything. | Dropped the amount/fraction requirement from the swap-recognition condition — naming a token or a token pair is now enough, with a `!any(text, "can i swap", "is swap", ...)` guard (symmetric to the existing borrow-branch pattern) so a bare capability question still doesn't get treated as an instruction. The write branch already handled a null amount gracefully (`requires_amount: true`). | vanna-copilot-orchestrator | ✅ Fixed, tested (3 new unit tests), verified live — now answers "How much do you want to swap? e.g. 'swap 20 XLM to...'" instead of the blurb |
| 22 | **Reported live** — issue #11, three related routing gaps under the same "everyday phrasing, no fixed word matched" theme: (a) "Transfer Collateral Margin to Wallet 20 XLM" named none of "withdraw"/"take out"/"pull" (only "collateral"), so the same instruction as "withdraw 20 XLM collateral" was never recognised as a write at all; (b) "tell me my margin account details" named none of "position"/"holdings"/"supply"/"net worth" either, so a request for the full account picture fell to the blurb; (c) "How much Interest accrued till date in BLUSDC" — no tool in this deployment tracks accrued interest separately from principal, and the question named none of "debt"/"owe"/"borrowed" so it wasn't even recognised as an account question. | (a) Added "transfer"/"move"/"send" as everyday synonyms for the same withdraw-collateral verb. (b) Added an `/account\s+details?/i` shape match to the holdings fan-out, answering with the same full HF/collateral/debt/net-value card as "what are my positions". (c) Added a new `query_accrued_interest` route + `accruedInterestAnswer` handler that answers honestly from the current debt figure for the named asset, with an explicit note that interest isn't tracked separately — never fabricating a number no tool provides. | vanna-copilot-orchestrator | ✅ Fixed, tested (5 new unit tests), verified live — "Transfer Collateral Margin to Wallet 5 XLM" executed a real 5 XLM withdrawal on-chain, "tell me my margin account details" answers the full account card, "how much interest accrued...in BLUSDC" answers "Your current BLUSDC owed is $183.10" with the no-separate-tracking note |
| 23 | **Reported live** — issue #14: "How much collateral do I have?" — one of the product's own suggested "my account" prompts — fell through to the generic capabilities blurb. Same fixed-phrase gap as debt/supply before it: the list matched "my collateral"/"collateral value" but not this word order. | Same shape-based fix as `asksAboutOwnDebt`/`asksAboutOwnSupply`: a quantity-question word or "my" near "collateral", not an exact phrase. | vanna-copilot-orchestrator | ✅ Fixed, tested (2 new unit tests), verified live — now answers "Your collateral: 1,228.8657 SOUSDC ($1229.30), ... — $2731.07 in total." |
| 24 | **Reported live** — issue #15: "Why is it showing 'Repay 2 USDC'? When I asked 'How much do I owe?'" The debt/collateral snapshot answer (behind `query_debt`/`query_collateral`) never populated `intent.slots.amount`/`symbol` at all, so the client's `followUpFor` — which already has logic to build an accurate "Repay/Borrow/Lend X SYMBOL" from real slots (a prior fix's own doc comment: "the follow-up must offer what the user actually asked about") — always fell back to the static `FOLLOW_UP` map's canned `"Repay 2 USDC"`, with no relation to the real (3-asset, $337.21) total just shown above it. | `snapshotPositionAnswer` now populates real `amount`/`symbol` slots when the question narrows to exactly one asset (so a focused ask like "how much AQUSDC do I owe" gets an accurate follow-up); removed the `vanna_get_debt: "Repay 2 USDC"` canned fallback entirely, since a multi-asset total still has no single figure to suggest and a fabricated-looking specific number was worse than no suggestion. | vanna-copilot-orchestrator | ✅ Fixed, tested (2 new unit tests — real slots for a single-asset focus, no fabricated slots for a multi-asset total), verified live — "how much do I owe" no longer offers "Repay 2 USDC" |
| 25 | **Reported live** — a related pair of precision bugs surfaced while investigating #23/#24: the readable answer's prose showed the RAW on-chain amount verbatim ("1228.8656935 SOUSDC" — 7 decimals), while the facts card underneath it ROUNDED the same figure to 4 decimals ("1,228.8657") — backwards from what a user wants: a clean rounded sentence, with the exact figure available in the card for anyone who needs it. | `listPositionRows` (the prose) now rounds through the existing `fmtPosAmount` helper; `prettyVal` (the facts-card formatter, `copilot-workspace.tsx`) now preserves the full fractional string (grouping only the integer part) whenever a value has more than 4 real decimal digits, instead of rounding it away — every other fact (money, percentages, HF) has ≤4 decimals already and is untouched. | vanna-copilot-orchestrator | ✅ Fixed, verified live — prose now reads "1,228.8657 SOUSDC", the facts card shows the exact "1,228.8656935" |
| 26 | **Reported live** — issue #13: "Can you Add Liquidity in Aquarius Pool in farm" (no tokens/amounts named) correctly asked "how much of each token?" but gave no sense of the right proportion, unlike the real Aquarius pool page's own "1 XLM ≈ 0.01 AqUSDC · 1 AqUSDC ≈ 71.64 XLM". | The amount-less `add_liquidity` clarify now fetches the live XLM/AQUSDC pool reserves via `AquariusService.getAquariusPoolStats` — the exact same function the Aquarius pool page itself calls, so it can never disagree with what the user sees there — and appends the ratio, best-effort (Aquarius-only, the one case this clarify names; a failed read still falls back to the plain question). | vanna-copilot-orchestrator | ✅ Fixed, verified live — now appends "Current pool ratio: 1 XLM ≈ 0.0140 AQUSDC · 1 AQUSDC ≈ 71.64 XLM", matching the real Aquarius pool page's own numbers exactly. Cross-checked per the user's own request against the Farm page's Positions tab, which shows the same reserves/venue and confirms no Aquarius LP position currently exists on this account (0 facts is the correct answer, not a missed one). |
| 27 | **Reported live** — issue #17: "I have to Faucet AQUSDC" / "Fucet" (typo) fell through to the generic capabilities blurb — testnet funding is a client-side nav action, not an MCP tool, so nothing recognised the word at all. | Added a shape match (`/\bfa?ucet\b/i`, tolerating the one-letter "Fucet" slip) that answers with plain guidance to the real Faucet button in the top nav, rather than the generic blurb. | vanna-copilot-orchestrator | ✅ Fixed, tested, verified live — answers "Testnet funding is the Faucet button in the top nav... Click it to fund your wallet with testnet XLM." |
| 28 | **Reported live** — issue #18: "My farm position" answered with a card explicitly badged **MARGIN ACCOUNT** and a note admitting "Blend supplies and Aquarius LP shares stay on Farm" — the same class of bug Earn positions had (finding #13), now reported for Farm. Root cause: "farm" was deliberately routed to the whole-account fan-out (the theory being its farm-overview MCP call covered it) but that call only ever contributes a best-effort PROSE sentence, never structured facts, so a real Blend/Aquarius/Soroswap LP position never showed up as a fact at all. A first attempt reused `getLitePositionsFromChain` (the Lite-mode leveraged-position tracker) — live-verified this answered **"$0.00"** for a real ~$49.86 Blend BLUSDC supply, because that function nets farm supply against SmartAccount margin debt in the same asset (the right number for "what's my net exposure on this leveraged position", the wrong one for "how much do I have"). | Added a dedicated `query_farm_position` route (same "names one venue" pattern as Blend/Aquarius/Earn) and a new `farmPositionAnswer` handler that reads GROSS balances directly from `BlendService`/`AquariusService`/`SoroswapService` — the same on-chain calls `getLitePositionsFromChain` itself makes, just without its debt-netting step — answering with `venue: "none"` (spans multiple venues) and never falling back to margin data. | vanna-copilot-orchestrator | ✅ Fixed, tested (5 new unit tests, including a regression pinning the exact $0.00-netting bug), verified live — "my farm position" now answers "49.8607 BLUSDC ($49.90)", matching the real Farm page's Positions tab exactly ("49.8607014 BLUSDC") |
| — | **Reported live, a general principle, not a single phrase** — typing just "SOUSDC" (answering a swap's "which token did you mean?") fell through to the generic capabilities blurb. The user's own framing: any bare recognized asset name should ask what to do with it, "unless it's outside our domain" — never read as gibberish just because it named no verb. | Added a check, early in `routeMessage`, for a message that is EXACTLY one known asset symbol and nothing else: answers "What do you want to do with {ASSET}?" with example actions, instead of falling through everything to the blurb. Deliberately narrow (exact match, not "contains an asset word") so it cannot steal a real instruction like "lend SOUSDC". | vanna-copilot-orchestrator | ✅ Fixed, tested (4 new unit tests), verified live — "SOUSDC" now answers "What do you want to do with SOUSDC? e.g. 'lend 10 SOUSDC'..." |

| 29 | **Reported live, the most serious report this session** — issue #16: "Swap 10 XLM to BLUSDC then farm Blend at 2x with 10 BLUSDC" correctly built a plan and correctly paused leg 1 (BLUSDC can't be swapped into on any AMM), but answering the pause with the corrected token ("SOUSDC") sent a brand-new, context-free message instead of resuming — the router could not understand "SOUSDC" alone and answered with the generic capabilities blurb, silently abandoning the paused strategy. Separately, once a plan DID reach `deploy_to_blend`, a non-Blend asset (AQUSDC/SOUSDC) was silently coerced into Blend's own USDC reserve instead of refused — the confirmed root cause of "chose Soroswap, got a Blend transaction instead." A full agent trace (RC1–RC5) mapped five contributing bugs across the client and server. | Fixed all five: (1) `mcp-write.ts`'s `deploy_to_blend`/`supply_to_blend` now refuses AQUSDC/SOUSDC instead of coercing them to Blend's USDC (finding above, already shipped). (2) `resume_multi_leg`'s handler now carries every executable slot (`toSlots`, matching a sibling resume path that already did this) instead of only `asset`/`amount`/`leverage` — a swap's `token_out` no longer vanishes on resume. (3) `token_in`/`token_out` threaded through the entire `MultiLegStep` wire type and every leg-status push site that lacked them, so the client can see and correct a swap's destination. (4) The composer detects a currently-paused swap leg and, when the typed text resolves to a known token, resumes THAT leg with the corrected `token_out` (`submitLegTokenAnswer`) instead of firing a fresh, context-free message. (5) A new `staticStepBlocker` (shared — `mapOpToMcpStep`'s own per-op blockers now delegate to it, so the two can never disagree) checks every step of a proposed plan for asset/op combinations that can NEVER succeed, refusing the whole plan upfront instead of showing a multi-step "Approve & run" card destined to pause one signature in. | vanna-copilot-orchestrator | ✅ Fixed, tested (9 new unit tests across the five fixes), verified live end-to-end: "swap 10 XLM to BLUSDC then farm Blend at 2x with 10 BLUSDC" is now refused immediately with the real reason, no plan card shown at all; a variant that pauses mid-plan and is answered "SOUSDC" now actually executes the swap on-chain to SOUSDC (real tx hash captured) and continues the strategy, instead of abandoning it |

| 30 | **Reported live, six separate bugs under one round.** (a) "give my margin account details" listed farm-venue LP/receipt tokens (`BLEND_USDC`, `SS_XLM_USDC`, `BLEND_XLM`) as `collateral · X` facts, mixed into the same list as real collateral — `isTrackingSymbol` already existed to tell them apart and was already used correctly one function over (`focusPositionRows`), just never applied on the "list everything" path; the same answer also never showed an Aquarius LP position, because `aquariusLpCollateralRow` (`farmTrackingCollateral.ts`) read only the Registry tracking-token balance (which goes stale) instead of falling back to the pool contract's own `get_user_shares()`, the way the Farm page and `farmPositionAnswer` already do. (b) "Give my Soroswap/Aquarius Farm Position Details" fell to the generic capabilities blurb — the `query_farm_position` route excluded any message naming a venue, on the theory that a named venue "already has its own more precise answer"; no such per-venue route exists for Aquarius/Soroswap (or Blend). (c) A staged write's `SUMMARY` fact (e.g. "Swap 10 XLM for at least 1…") was visibly clipped mid-sentence — `Row()`'s value column applied Tailwind `truncate` unconditionally to every non-identifier value, with no length check. (d) "What is my XLM Balance in Margin account?" answered that only the smart-account address was resolved, no balances — `query_resolve`'s match (`"margin account"` + `"what is my"`) is broad enough to swallow any possessive question mentioning "margin account", because no earlier branch had a pattern for "`<asset>` balance ... in margin account" at all. (e) "Deposit 100 XLM and Borrow BLUSDC & AQUSDC at 3x Leverage" settled leg 1 (deposit) on-chain, then leg 2 (borrow) was immediately rejected by the risk engine's own on-chain pre-flight (`is_borrow_allowed`) — it read the account's collateral before the deposit's own ledger had closed. The real Margin page never hits this: its sequential deposit-then-borrow blocks on `pollTransactionStatus` between legs for exactly this reason; the copilot's multi-leg loop advanced to the next leg's pre-flight the instant MCP returned a `tx_hash`, with no equivalent wait. (f) An executed receipt showed two redundant facts cards — one hand-built (tx hash/status/mcp tool/signer) that duplicated the "Agent run" timeline immediately above it verbatim, and the raw-data card below it padded with `amount_stroops`/`session_id`/a duplicate `status`, while its own tx hash (in the first card) was truncated. | (a) `allPositionsStructured` now splits `pos.collateral`/`pos.borrowed` via `isTrackingSymbol`, routing tracking rows into a new `AnswerFact.group: "lp"` that `AnswerView` renders in its own boxed section (mirroring the existing figures/identifiers split), labelled via the existing `positionRowLabel` humaniser; `aquariusLpCollateralRow` now calls the same fallback-capable `AquariusService.getUserLpBalance` the Farm page uses. (b) The `query_farm_position` route now extracts a named venue (`blend`/`aquarius`/`soroswap`) instead of excluding it, and `farmPositionAnswer` takes an optional `venue` param that narrows its Blend/Soroswap/Aquarius fetches and headline to just that venue. (c) `Row()` now renders any non-identifier value over 40 characters in the same full-width, wrapping, un-clipped layout identifiers already use, instead of a fixed-width `truncate`. (d) `asksAboutOwnCollateral` now also matches "balance" co-occurring with "margin account", ahead of `query_resolve` in routing order, reusing the existing `positionAssetFocus` asset-narrowing already built for collateral/debt questions. (e) New `waitForLedgerClose` polls `getTransaction` on the just-settled leg's own hash (mirroring `pollTransactionStatus`) before the next health-affecting leg's action runs, whenever a further leg is still queued; best-effort, capped, never blocks a plan on a wedged RPC. (f) Removed the redundant hand-built card entirely; added `status`/`amount_stroops`/`session_id` (both underscore and the server's space-humanised spelling) to `FactsGrid`'s existing `skipKeys` set, leaving only `final status` and the already-full `tx hash`. | vanna-copilot-orchestrator | ✅ Fixed, tested (18 new unit tests across all six), verified live: margin-account-details now shows a separate "LP / farm positions" box (cross-checked against the real Farm page's Positions tab — same $59.91 Blend USDC supply, zero Soroswap/Aquarius LP on both); venue-scoped farm-position queries answer correctly for Soroswap and Aquarius (also cross-checked against the Farm page); the XLM-balance question now answers "You have 6,995.1535 XLM ($1077.05) of collateral" instead of the resolve-only message; the executed receipt now shows exactly `tx hash` (full) and `final status`, no redundant card |

| 31 | **Reported live, safety-critical + three routing gaps under one round.** (a) A focused single-asset question ("What is my XLM Balance in Margin account?") answered correctly in PROSE but its facts card still dumped health factor, debt, net value, both liquidation figures, and every other asset's amount — the prose was narrowed, the card never was, the same "gross amount only" violation already fixed once for named single-figure margin questions. (b) **Safety-critical** — "Can You Remove 50 BLUSDC fom Farm's Blend Pool" staged, and (live-reproduced) actually EXECUTED, a real SUPPLY of 50 BLUSDC to Blend instead of a withdrawal. Root cause was two independent, undocumented-to-each-other regexes: `router.ts`'s Blend-write matcher fired on venue signals alone ("blend pool" substring, "farm's" containing "farm" as a whole word) with no removal-verb exclusion; separately, `handle.ts` re-derives its OWN "is this a Blend write" classification from the raw message and unconditionally overrides the router's decision when it fires — so fixing the router alone was not enough, the second copy had to be fixed too, and no `withdraw_from_blend` op existed anywhere in the codebase to route TO. (c) "What is my TVL in Farm Section" fell to the generic blurb — TVL is the Farm page's own label for the exact total `farmPositionAnswer` already computes, just an unrecognised synonym. (d) "What is XLM to SoUSDC Ratio in farm Soroswap pool?" fell through entirely — no route ever answered an AMM pool's live reserve ratio directly (only a narrow Aquarius-only side note on a blocked add_liquidity clarify existed). (e) "What is Current Rate of bXLM?" was rejected by the domain firewall as off-topic, and even after that fix was answered by the generic Guide concept-explainer instead of the real number — bXLM (Blend's own bToken symbol) was missing from **three independent** asset-recognition lists (the domain firewall's vocabulary, the asset registry's XLM aliases, and `concept.ts`'s `ASSET_SYMBOL` live-data override), each with the same root cause: `\bXLM\b` can never match inside the unspaced composite "bXLM". | (a) `snapshotPositionAnswer`'s `data` payload now branches on whether the question resolved to a single `focusedRow`: when it did, the facts card carries only that one asset (via the same array-flatten shape `factsForUi` already uses for the whole-account case, just with a 1-item array); the unfocused case is unchanged. (b) Added a real `withdraw_from_blend` op end-to-end: a removal-verb check (`remove/withdraw/take out/pull out/unwind/redeem`) in BOTH of `router.ts`'s Blend-write blocks, checked ahead of the supply match and widened to recognise "from Blend" as a venue signal (the existing list only had "to/into/on Blend"); the SAME removal-verb exclusion added to `handle.ts`'s independent `blendWrite` regex, with an explicit early branch honouring a correctly-routed `withdraw_from_blend` before that regex chain runs; a new `mapOpToMcpStep` case emitting the already-registered-but-unreachable `vanna_blend_withdraw` MCP alias; plan-card label/venue-suffix entries so a withdraw never reads "into Blend". (c) Added `tvl`/`total value locked` as a holdings-question synonym. (d) New `query_pool_ratio` route + `poolRatioAnswer` handler reading `SoroswapService.getPoolStats()` / `AquariusService.getAquariusPoolStats()` directly — the same live reserves the LP pool pages themselves read. (e) Added `bxlm`/`busdc` to the domain firewall's `DOMAIN_WORDS`, to XLM/BLUSDC's alias lists in the asset registry, and to `concept.ts`'s `ASSET_SYMBOL` regex. | vanna-copilot-orchestrator | ✅ Fixed, tested (22 new unit tests across all five), verified live: the XLM-balance card now shows only `{"XLM amount": ..., "asked about": "XLM"}`; the exact reported Blend-removal sentence now calls `vanna_blend_withdraw` and correctly labels "Withdraw 50 BLUSDC from Blend" (confirmed via a live on-chain execution, which also happened to reverse the earlier accidental supply from the pre-fix repro); "What is my TVL in Farm Section" answers "$59.90 total", matching the Farm page's own "Your Deposit TVL" stat; the Soroswap ratio question answers "1 XLM ≈ 0.0680 SOUSDC · 1 SOUSDC ≈ 14.7109 XLM" from live reserves; "What is Current Rate of bXLM?" now answers with the real Blend XLM reserve stats (b rate 1.79, supply/borrow APY, utilization) instead of a generic explainer paragraph |

| 32 | **Reported live, four LP-related fixes plus a formatting nit.** (a) The pool-ratio headline crammed both directions into one run-on sentence with a middle-dot separator ("1 XLM ≈ 0.0680 SOUSDC · 1 SOUSDC ≈ 14.7109 XLM"), hard to scan. (b) "Can you add 10 XLM and 10 AqUSDC in Aquarius Pool in farm" staged the two amounts verbatim with zero regard for the pool's live reserve ratio — a real AMM add only works at the current ratio (confirmed: neither the copilot's own step-builder nor the on-chain `addLiquidity` call apply any ratio correction or slippage protection), while the real Aquarius/Soroswap add-liquidity form on this site never lets a user set both sides independently for exactly this reason. (c) Farm positions showed only the underlying token split for an LP holding ("28.904 XLM + 1.9678 USDC"), never the actual LP share count held. (d) "Can you remove 10 LP from farm Aquarius XLM and USDC Pool" fell through every deterministic route (the `remove_liquidity` gate only recognised "remove liquidity" as an adjacent phrase, or a bare "half"/"50%" fraction — no amount-based clause existed the way `add_liquidity`'s own gate has) and reached an LLM that free-generated a confused "withdraw from your margin account collateral, or check your Aquarius LP position?" clarify with no deterministic route to anchor it. (e) One of the product's own suggested prompt chips — "Swap 10 XLM to BLUSDC then farm Blend at 2x with 10 BLUSDC" — was itself the exact statically-impossible plan already refused by finding #29/#30's `staticStepBlocker` fix, so the product was suggesting an action it would then reject. | (a) `poolRatioAnswer`'s headline now states only the asked-for direction; the reverse direction lives in its own fact row, which already existed. (b) Before calling `mapOpToMcpStep` for `add_liquidity` with both amounts stated, `runWrite` now reads the same live pool reserves `poolRatioAnswer` uses (Aquarius or Soroswap by venue) and recomputes the second amount from the first at the pool's actual ratio when the stated amount is off by more than 1%, with a note explaining the correction — mirroring what the site's own add-liquidity form already does client-side. (c) `farmPositionAnswer`'s Soroswap/Aquarius LP facts now append the raw LP share count (`· N LP`) alongside the existing underlying-token split. (d) Added an amount-based clause to the `remove_liquidity` gate (`remove`/`withdraw`/`take out`/`pull out` + `lp`/`liquidity`/a named venue, excluding `collateral`), symmetric to `add_liquidity`'s own bare-verb-plus-venue shape; also fixed the venue-vs-bare-"USDC" resolution so "Aquarius ... USDC Pool" resolves to AQUSDC, not generic USDC. (e) Replaced the impossible prompt chip with a strategy that actually executes ("Swap 10 XLM to AqUSDC then add liquidity in Aquarius"). | vanna-copilot-orchestrator | ✅ Fixed, tested (5 new unit tests), verified live: the ratio question now reads cleanly on one line; the exact reported add-liquidity sentence corrected 10 AQUSDC down to ~0.1395 AQUSDC and executed on-chain to the pool's real ratio; farm positions now show "· 1.1761 LP" alongside the underlying split; the exact reported remove sentence now calls `vanna_remove_liquidity` (not a confused clarify) and executed on-chain; the Prompts panel no longer offers the impossible swap-to-BLUSDC-then-farm-Blend chip |

**Found in the same investigation, MCP-server-side — fixed.** Calling `vanna_get_vtoken_balance` more than once in quick succession (whether in parallel or sequential) reliably failed with "operation was aborted due to timeout" client-side, even though a single isolated call reliably succeeded in ~10s. Confirmed via `gcloud logging read` on `vanna-mcp-server`'s own Cloud Run logs: `WARNING: Truncated response body. Usually implies that the request timed out or the application exited before the response was finished.`, timestamped at the exact moments the client-side timeouts occurred, alongside bursts of 40+ `POST https://soroban-testnet.stellar.org` calls within a single second. The tool itself does 3-4 sequential Soroban contract calls per invocation (`balance`/`total_supply`/`decimals` in parallel, then `convert_vtoken_to_asset`, then `symbol`) — genuinely expensive by design, and the Cloud Run container was getting overwhelmed under even modest concurrent/repeated load at its prior 512Mi/1 vCPU allocation. Fixed via `gcloud run services update --memory=2Gi --cpu=2` (new revision `vanna-mcp-server-00079-lnp`, confirmed serving 100% traffic) — all 4 sequential calls now complete reliably.

**Verified live, full pass across every Earn pool (issue #7's "haven't gone through every pool's detail page and Supply/Withdraw flow yet").** Walked every asset in both the Vaults tab and the Positions tab, opened each pool's Details page, and ran real Supply Liquidity + Withdraw Liquidity transactions on XLM end-to-end (auto-sign on, real testnet wallet). Every figure matched copilot's `query_earn_position` answer exactly: XLM 20.02 ($3.16-3.18, oracle-price drift), AqUSDC 14.99 ($15.00), SoUSDC 52.00 ($52.04), BLUSDC 0 (correctly absent from both the Positions tab and copilot's answer). Supplying 1 XLM raised "Your Supply" 20.02→21.02 and the pool's Total Supply $11.54K→$11.57K, both without a reload; withdrawing that same 1 XLM (as vXLM) correctly reversed it, though the Withdraw panel's own "Balance: X vXLM" figure and the Details tab's Total Supply stat did not visibly update until a full page reload — **not confidently called a bug**, since `withdraw-liqudity.tsx` already re-syncs on a staggered 0/3/6/10/15s schedule specifically to avoid needing a reload, reads the balance via a direct Soroban RPC simulate call that can lag ledger finality by a few seconds, and this was checked well within that window rather than after it fully elapsed. Worth a second look if a user reports it again with a longer wait.

**Investigated, believed already fixed — could not get final confirmation from the affected account.** Collateral shown by copilot for one specific account (dual-borrow BLUSDC+AQUSDC, 3x leverage) was reported reading ~$631 vs the correct ~$473 (health factor 2.00 vs the correct 1.50) — the historical "borrowed BLUSDC/AQUSDC sitting in the margin account counted as collateral too" bug class (see `lib/account-snapshot.ts`'s existing comments on this). Not reproducible on any real account checked this pass — every collateral/debt/health-factor figure matched ground truth exactly on current code, including one account (`CATLUTO4...WCGK`) whose C-address was visible in a screenshot the user shared: current code shows $688.01 collateral / $357.28 debt / HF 1.93 with no discrepancy. The screenshots showing the ~$631 mismatch are most likely from a **stale Vercel preview deployment** predating this session's fixes (the same deployment also still shows the old `clarify_usdc_variant` Earn-supply bug from #9/#13 above, which current code no longer has). Worth a redeploy + one more live check on the specific reporting account before considering this fully closed.

**Reported live, not yet reproduced:** after depositing on the Margin page, the Copilot page's account sidebar (health factor / collateral / debt) doesn't reflect the new state, even after running a copilot prompt. Traced the refresh path: `/api/account/[addr]` already sets `Cache-Control: private, no-store, max-age=0` (with a comment noting this exact staleness class was fixed before), and the client calls a forced `refreshRailStats()` after every `executed`/`needs_wallet_sign` response. Could not reproduce end-to-end without a live signing session (a raw API call can stage a write but not sign it) — needs to know whether the report is about the sidebar after fully **approving/signing** a write, or after only **running** (staging) one, since the staged-but-unsigned state correctly shows no change yet.

**Pre-existing, unrelated to this pass:** `vanna_mcp`'s working tree already had uncommitted changes to `mcp_server/tools/sign_tools.py`, `mcp_server/tools/borrow_tools.py`, and a deleted `ALLOWLIST_FIX_PLAN.md` before this session touched the repo — left untouched. Two tests in `tests/test_mcp_sign_tools.py` (`test_enable_auto_sign_success_forwards_and_summarizes`, `test_enable_auto_sign_forwards_optional_caps_and_expiry`) fail against that uncommitted state (an in-progress `function_allowlist` change), unrelated to anything in this document.

---

## 4. Housekeeping (No Urgency)

| # | Item | Action |
|---|------|--------|
| 1 | 66 dependabot vulnerabilities (32 high) on the default branch. | Schedule a dependency-upgrade pass. |
| 2 | `note.txt` §18's expected test count (778) is stale. | Update to 885. |

---

## 5. Still Needs Testing (Coverage Gap, Not a Known Bug)

| # | Area | Blocked By |
|---|------|------------|
| 1 | Sections 1–9 with auto-sign **OFF** | This pass ran almost entirely with auto-sign ON. |
| 2 | K-01–K-04 | Require the W3 thin/near-liquidation wallet — not authorised this session. |
| 3 | Z-01, Z-03–Z-06 (kill network, disconnect/switch wallet, `COPILOT_READS_ONLY`, wrong network) | Require capabilities this session didn't have (network throttling, a second wallet, an env-var restart). |
| 4 | S-08 (reject the wallet signature prompt) | Requires a Freighter-style external wallet — this account signs via an embedded Privy session key with no separate prompt to reject. |
